# Processed Data

This project applies preprocessing (resizing, normalization, augmentation)
on-the-fly at training/inference time using `training/transforms.py`.

No static processed image files are saved to disk — this folder is kept
for structural consistency with the project template.